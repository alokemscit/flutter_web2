import 'package:flutter/material.dart';

class ReportGroupSetup extends StatelessWidget {
  const ReportGroupSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}